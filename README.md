# Parker Product Studio

Landing site and API for Parker Product Studio — a React (Vite) frontend and Express backend in an npm workspaces monorepo.

## Scripts

- `npm install` — install dependencies
- `npm run dev` — run client and server in development
- `npm run build` — production build
- `npm start` — run the production server
